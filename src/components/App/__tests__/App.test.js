import Root from '../../../Root';
import App from '../';

import Shelf from '../../Shelf';
import FloatCart from '../../FloatCart';

let wrapped;
describe('App', () => {
  it('app', async () => {
    expect(0).toEqual(0);
  });
});
